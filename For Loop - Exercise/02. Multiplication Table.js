function multiplication(number){
    
    for (let multiplication = 1; multiplication <= 10; multiplication++) {
        console.log(`${multiplication} * ${number} = ${multiplication * number}`);
    }
}